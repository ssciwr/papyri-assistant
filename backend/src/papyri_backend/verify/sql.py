def sql_query_verifier(query):
    # print(query)
    # forbidden
    # if ";" in query.rstrip(";"):
    #     raise ValueError("One sql statement only")

    # if not query.upper().startswith("SELECT "):
    #     raise ValueError("Only SELECT queries are allowed")

    # check syntax validation
    return True
